# CW-01 — Company Workspace (short)

Primary company UX. Assembles existing intelligence (FIRE-01…06, IO-01, WO-01, PO-01) via Office SDK contracts. Subscribes to PEB for view refresh only.

- Package: `company_workspace/`
- Spec: `docs/CW_01_COMPANY_WORKSPACE.md`
- CLI: `python -m company_workspace --company TCS`
- API: `GET /v1/company-workspace/{ticker}` (+ `/timeline`, `/research`, `/evidence`)

Not an engine. Not an office. Presentation only.
