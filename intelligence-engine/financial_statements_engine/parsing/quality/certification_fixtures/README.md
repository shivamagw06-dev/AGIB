# Parser certification fixtures (FSE-04.1)

Reference filings + expected outputs for certification gates.

Add new fixtures as `*_min.json` + `expected_*.json` pairs and register them in `certification.py`.
