# ICE Architecture

```text
InstitutionalAnswer
  → Planner (audience / template / densities)
  → Template structure
  → Bind evidence / frameworks / risks / confidence / citations
  → Quality gates
  → User-facing prose
```

Renderer only. Starts from InstitutionalAnswer — never from the raw question alone.
