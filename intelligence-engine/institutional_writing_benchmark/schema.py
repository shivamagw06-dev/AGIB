"""Institutional writing benchmark — schema (delegates to IIC)."""

from __future__ import annotations

from institutional_investor_curriculum.schema import (
    ANCHOR_COMPANIES,
    DECISION_DOMAINS,
    DOMAIN_TITLES,
    HALL_OF_FAME_COUNT,
    TARGET_BENCHMARK_COUNT,
    UNIVERSAL_QUESTION_COUNT,
)

# Back-compat aliases
BENCHMARK_CATEGORIES = DECISION_DOMAINS
LIFECYCLE_PLAYBOOKS = DECISION_DOMAINS
PLAYBOOK_TITLES = DOMAIN_TITLES
PLAYBOOK_COUNT = len(DECISION_DOMAINS)
QUESTIONS_PER_DOMAIN = 10
QUESTIONS_PER_PLAYBOOK = 10
PHASE2_COMPANIES = ANCHOR_COMPANIES[1:]  # legacy: non-TCS anchors
PHASE2_TARGET_BENCHMARK_COUNT = TARGET_BENCHMARK_COUNT
